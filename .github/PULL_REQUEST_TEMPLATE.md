## What this changes

<!-- For a fix: what was wrong, and what the user saw. For a feature: what it
     now lets someone do. -->

## How it was verified

<!-- `./test-all.sh` is the gate CI runs. For anything visible, say what you
     did in the running editor. -->

- [ ] `./test-all.sh` passes
- [ ] Touched the Rust engine? The rebuilt `packages/editor/engine/pkg/` is
      committed alongside the `src/` change
- [ ] For a bug fix: a test that fails before this change and passes after it
